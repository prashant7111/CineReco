Runtime uses the prebuilt models/catalog and local poster assets. Raw source datasets are intentionally not bundled.


## v7
Movie detail-page fix: parsed tag lists are no longer treated as strings; genre/tag parsing is normalized. Default port 5077.
